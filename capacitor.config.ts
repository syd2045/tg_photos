import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sydstudio.tguploader',
  appName: 'Kirim ke Telegram',
  webDir: 'www'
};

export default config;
